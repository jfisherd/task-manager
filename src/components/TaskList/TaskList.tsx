
import type { TaskListProps } from '../../types'

export const TaskList = (props: TaskListProps) => {

    // useState variables go here?

    return (
        <>
            <ul></ul>
        </>
    )
}